{
  "name": "rexkit",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "npm install && npm install --prefix backend/crud-generator && npm install --prefix frontend",
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm run dev --prefix backend/crud-generator\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend/crud-generator",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "concurrently -n backend,frontend -c cyan,magenta \"npm start --prefix backend/crud-generator\" \"npm start --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
