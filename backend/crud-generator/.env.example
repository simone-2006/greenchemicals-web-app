# Copia questo file in .env e compila i valori.
# Il file .env non va committato: è già nel .gitignore.

# Porta backend (opzionale: se assente vale ports.json nella root)
# PORT=3000

# Connessione SQL Server
DB_SERVER=localhost
DB_PORT=1433
DB_DATABASE=nome_database
DB_USER=sa
DB_PASSWORD=
DB_ENCRYPT=false
